<UserControl 
    x:Class="Maths.WPF.Views.Home.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Maths.WPF.ViewModels.Home"
    mc:Ignorable="d"
    MinHeight="400"
    MinWidth="300">

    <UserControl.DataContext>
        <vm:HomeViewModel />
    </UserControl.DataContext>
    
    <Grid Style="{StaticResource GridApplication}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <!--Titre de la page-->
        <Border Grid.Row="0" Style="{StaticResource BorderHeader}">
            <Label Content="Accueil" Style="{StaticResource LabelHeader}" />
        </Border>

        <!--Corps de la page-->
        <Border Grid.Row="1" Style="{StaticResource BorderBody}" />

        <!--Actions-->
        <Border Grid.Row="2" Style="{StaticResource BorderFooter}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Paramétrer test" Command="{Binding AccessToSetupCommand}" />
                <Button Content="Quitter" Command="{Binding CloseApplicationCommand}" />
            </StackPanel>
        </Border>
    </Grid>

</UserControl>