<Window x:Class="PresentationLayer.Wpf.View.DialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        Title="{Binding DialogTitle}" Height="{Binding DialogHeight}" Width="{Binding DialogWidth}">

    <ContentControl x:Name="Pages" DockPanel.Dock="Right" Content="{Binding DialogContentViewModel}"/>

</Window>